{
  "name": "chess-coach-e2e",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:web": "E2E_TARGET=web-no-llm npx playwright test",
    "test:desktop": "E2E_TARGET=desktop npx playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^24.12.2",
    "chess.js": "^1.4.0",
    "typescript": "~5.9.3"
  }
}
