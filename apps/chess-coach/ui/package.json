{
  "name": "chess-coach-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:web": "VITE_TARGET=web vite",
    "dev:desktop": "VITE_API_URL=http://localhost:8080 vite",
    "build": "tsc -b && vite build",
    "build:web": "tsc -b && VITE_TARGET=web vite build",
    "build:desktop": "tsc -b && vite build",
    "preview": "vite preview",
    "test:int": "vitest tests/"
  },
  "dependencies": {
    "chess.js": "^1.0.0-beta.8",
    "solid-js": "^1.9.10",
    "@mlc-ai/web-llm": "^0.2.81"
  },
  "devDependencies": {
    "@solidjs/testing-library": "^0.8.10",
    "@types/node": "^24.10.1",
    "jsdom": "^28.1.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vite-plugin-solid": "^2.11.10",
    "vitest": "^4.1.0"
  }
}
